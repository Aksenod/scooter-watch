# ScooterWatch - Платформа для отчетности о нарушениях на самокатах

## 🚨 Важно: Исправление ошибки 404 при входе

### Проблема
При попытке войти в приложение возникает ошибка:
```
your-backend.onrender.com/api/auth/otp-verify:1  Failed to load resource: the server responded with a status of 404 ()
```

### Причина
Приложение деплоится на **GitHub Pages** как статический сайт, но API routes Next.js **не работают на статических хостингах**. Текущая конфигурация пытается обратиться к несуществующему серверу `https://your-backend.onrender.com`.

### Решение
Нужно деплоить всё приложение на **Render** (или аналогичную платформу) как полноценный Next.js сервер, чтобы API routes работали.

📖 **Подробная инструкция:** [RENDER_DEPLOYMENT.md](./RENDER_DEPLOYMENT.md)

## Быстрый старт для деплоя

1. **Создайте аккаунт на [Render](https://render.com)**

2. **Создайте PostgreSQL базу данных:**
   - New + → PostgreSQL
   - Name: `scooter-watch-db`
   - Plan: Free
   - Скопируйте Internal Database URL

3. **Деплойте приложение:**
   - New + → Blueprint
   - Подключите этот репозиторий
   - Render автоматически использует `render.yaml`
   - Добавьте переменную окружения `DATABASE_URL` со значением из шага 2

4. **Примените миграции:**
   ```bash
   cd scooter-watch
   npx prisma migrate deploy
   ```

5. **Готово!** Приложение доступно по адресу `https://scooter-watch.onrender.com`

## Структура проекта

```
scooter-watch/
├── scooter-watch/          # Основное Next.js приложение
│   ├── app/               # Next.js App Router
│   │   ├── api/          # API routes (OTP, Reports, Wallet)
│   │   ├── auth/         # Страницы авторизации
│   │   ├── record/       # Страница создания отчета
│   │   ├── history/      # История отчетов
│   │   └── wallet/       # Кошелек пользователя
│   ├── features/         # Модули по фичам
│   ├── lib/             # Утилиты и сервисы
│   └── components/      # UI компоненты
├── prisma/              # Схема базы данных
├── render.yaml          # Конфигурация для Render
└── .github/workflows/   # GitHub Actions (для Pages)
```

## Локальная разработка

1. **Установите зависимости:**
   ```bash
   cd scooter-watch
   npm install
   ```

2. **Настройте базу данных:**
   ```bash
   # Создайте .env.local файл
   echo "DATABASE_URL=postgresql://user:password@localhost:5432/scooter_watch" > scooter-watch/.env.local

   # Примените миграции
   cd scooter-watch
   npx prisma migrate dev
   ```

3. **Запустите dev сервер:**
   ```bash
   npm run dev
   ```

4. **Откройте http://localhost:3000**

## Технологии

- **Frontend:** Next.js 14, React, TypeScript, Tailwind CSS
- **Backend:** Next.js API Routes, Prisma ORM
- **Database:** PostgreSQL
- **Auth:** OTP (SMS) аутентификация
- **AI:** OpenAI GPT-4 Vision для классификации нарушений
- **Deployment:** Render (Full-stack) или GitHub Pages (Frontend only)

## Основные функции

- 📱 OTP аутентификация по номеру телефона
- 📸 Загрузка фото/видео нарушений
- 🤖 AI классификация нарушений (GPT-4 Vision)
- 📍 Геолокация нарушений
- 💰 Система вознаграждений (20% от штрафа)
- 👛 Кошелек для вывода средств
- 📊 История отчетов и статусы

## Варианты деплоя

### Вариант 1: Render (Рекомендуется) ✅
- ✅ API routes работают
- ✅ Полный функционал
- ✅ Бесплатный план доступен
- ⚠️ "Засыпает" на free плане

### Вариант 2: GitHub Pages + Отдельный Backend
- ✅ Фронтенд всегда активен
- ❌ Требует настройки отдельного backend
- ❌ Нужна настройка CORS
- ❌ Сложнее в поддержке

## Лицензия

MIT

## Поддержка

Если возникли вопросы или проблемы, создайте [Issue](https://github.com/Aksenod/scooter-watch/issues).

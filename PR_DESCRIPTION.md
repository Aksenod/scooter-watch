# Pull Request: Упрощённая авторизация без OTP для GitHub Pages

## 🐛 Проблема

При попытке войти в приложение возникала ошибка 404:
```
POST https://your-backend.onrender.com/api/auth/otp-verify 404 (Not Found)
Error: Network error
```

Приложение пыталось вызвать API endpoint для OTP верификации, который не существует на GitHub Pages (статический хостинг не поддерживает API routes).

## ✅ Решение

Реализована **упрощённая авторизация без OTP кода** для работы на GitHub Pages:

### Изменения в авторизации:
- ❌ Убрана двухэтапная авторизация (запрос OTP → ввод кода)
- ❌ Убраны вызовы API (`/api/auth/otp-request` и `/api/auth/otp-verify`)
- ✅ Добавлен простой вход по номеру телефона
- ✅ Данные пользователя сохраняются в localStorage
- ✅ Работает полностью на клиенте, без backend

### Пользовательский опыт:
1. Пользователь вводит номер телефона
2. Нажимает кнопку "Войти"
3. Автоматически создаётся пользователь в localStorage
4. Редирект на страницу /record
5. **Никаких API запросов, никаких ошибок!**

## 📁 Изменённые файлы

### Основные изменения:
- **app/auth/page.tsx** - упрощённая форма авторизации
- **scooter-watch/app/auth/page.tsx** - то же для build директории
- **README_RU.md** - обновлена документация

### Дополнительно (конфигурация для будущего):
- **render.yaml** - конфигурация для деплоя на Render (для полной версии с БД)
- **RENDER_DEPLOYMENT.md** - инструкция по деплою с backend
- **DEPLOYMENT.md** - добавлены предупреждения о GitHub Pages

## 🎯 Результат

После этого PR приложение будет работать на GitHub Pages **без ошибок**:
- ✅ Вход работает сразу по номеру телефона
- ✅ Нет ошибок 404
- ✅ Не требуется настройка backend
- ✅ Идеально для MVP/демо версии

## 🔄 Совместимость

Текущая версия - это MVP для GitHub Pages. В будущем можно:
- Развернуть полную версию на Render с базой данных (используя render.yaml)
- Вернуть OTP авторизацию при наличии backend
- Все конфигурации уже подготовлены

## 📝 Тестирование

После мержа и деплоя:
1. Откройте https://aksenod.github.io/scooter-watch/
2. Нажмите "Начать"
3. Введите любой номер телефона (например, +79991234567)
4. Нажмите "Войти"
5. ✅ Должен быть редирект на страницу /record без ошибок

---

**Fixes:** Ошибка 404 при входе в приложение
**Type:** Bug Fix + Feature
**Priority:** High
